// General class for managing pairs of values
class stzPair from stzObject
	aContent
	
	init(paList)
		// Control: paList must contain just 2 items
		for item in paList
			acontent + item
		ok

	def Content()
		return aPair

