# project-s3

> ترجمة أولية، في انتظار مراجعة متحدث أصلي.

عالم علّمته، يسأله وكيل تحكمه أنت.

## ما يتحقق منه الحارس

- يحوي مجلدك `world.zknw` (خمس حقائق على الأقل) و`agent.pia` (تقبله المحكمة، وكل أفعاله قابلة للتراجع) و`ask.ring`
- يشغّل الحارس `ask.ring` مع `$cProjectFolder` مضبوطًا على مجلدك
- يحمّل `ask.ring` عالمك ويطبع جوابًا واحدًا منه على الأقل
