# GO-02 · Propose, never commit

question: Who is allowed to make this real?
foundation: shows that an LLM actor is refused at MayCommit
practitioner: builds an agent graph where the LLM proposes and a pi-gate commits
expert: explains a violation the graph reports and fixes the wiring
