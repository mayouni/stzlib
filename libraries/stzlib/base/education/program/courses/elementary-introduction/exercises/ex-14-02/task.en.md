### Exercise 14.2 · Two shapes


Build two agent graphs: one where a language model proposes straight to an effect, and one where its proposal passes a gate, which feeds the effect, which is traced. Print whether each is sound, the ungoverned one first, on two lines.

Your answer is checked by running your program. Nobody reads it and compares it with a model answer.
