### التمرين 3.1 · دون مساس

> ترجمة أولية، في انتظار مراجعة متحدث أصلي.


ابدأ من `o1 = new stzString("rixxnxg")`. اطبع `RING` بجملة واحدة موجهة إلى `o1`، ثم اطبع محتوى `o1` لتثبت أنه لم يتغير.

يُتحقق من جوابك بتشغيل برنامجك. لا أحد يقرؤه ليقارنه بجواب نموذجي.
