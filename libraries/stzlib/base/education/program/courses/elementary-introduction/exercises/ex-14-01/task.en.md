### Exercise 14.1 · Declare a watcher


Declare, as text, an agent named `stock-watcher` that checks the kitchen stock every morning, with every act reversible. Print whether the court admits it, then its reversibility class, on two lines.

Your answer is checked by running your program. Nobody reads it and compares it with a model answer.
