# Narrative
# --------
# (retired) starts real libuv HTTP server (binds port + blocks). Requires RingLibuv.
# Integration test -- belongs under tests/integration/, not base/test/.
#
#SKIP retired -- see header

load "../../stzBase.ring"

? "(retired test; see header for rationale)"
