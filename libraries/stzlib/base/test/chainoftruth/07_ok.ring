# Narrative
# --------
# Extracted from stzchainoftruthtest.ring, block #7. The original block was
# commented out in the monolith too: the bare `expr._` chain-closer does not
# parse as a standalone statement in Ring (C27). Kept as documentation of the
# legacy form; see doc/design/NATURAL_VISION.md section 3.3 for the
# stzChainOfTruth verdict (absorb into interrogative narrations, or rebuild
# resolver-backed).

load "../../stzBase.ring"

pr()

# The legacy form this block documented (does not parse as a statement):
/*
? _(1854).IsA(:String).Containing('in')._
#--> 0
*/

? "(documented legacy form; see header)"

pf()
