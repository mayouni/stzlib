# Narrative
# --------
# (retired) uses JS-style hash-literals ('@list = {"type": "leaf", ...}') which are
# NOT valid Ring syntax. Pending listexuter DSL upgrade to accept this
# form. Retire until parser handles it.
#
#SKIP retired -- see header

load "../../stzBase.ring"

? "(retired test; see header for rationale)"
