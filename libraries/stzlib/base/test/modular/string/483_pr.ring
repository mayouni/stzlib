# Narrative
# --------
# pr()
#
# Extracted from stzStringTest.ring, block #483.

load "../../../stzBase.ring"


? Q(".;1;.;.;." ) / ";" # Same as: ? Q(".;1;.;.;." ).Splitted(:Using = ";")

#--> [ ".", "1", ".", ".", "." ]

pf()
# Executed in 0.01 second(s).
