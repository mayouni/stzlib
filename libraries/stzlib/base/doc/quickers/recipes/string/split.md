# Intent: Split a string into parts on a delimiter

```ring
Q("a,b,c").Split(",")
#--> [ "a", "b", "c" ]
```

- **Methods:** stzString.Split, stzString.SplitCS
- **Tags:** split, separate, tokenize, break apart, delimiter, parts, explode
- **See also:** replace
